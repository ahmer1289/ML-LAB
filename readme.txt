1. Virtual Machine Setup for Windows
    python3 -m venv venv
    .venv/Scripts/activate 

2. pip install -r requirements.txt
   pip install transformers torch
   pip install librosa soundfile pydub
   
3. python app.py

